{
  "name": "audio-loudnorm-online",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@10.33.1",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm --filter @audio-suite/web dev",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "format": "prettier --check ."
  },
  "devDependencies": {
    "@types/node": "^25.9.1",
    "prettier": "^3.6.2",
    "typescript": "^5.9.3"
  }
}
